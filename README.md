- Ir para pasta do projeto (Pelo terminal)
- No terminal: pip install -r requirements.txt
- Ainda na paster no projeto (Ainda no terminal) : python manage.py runserver
- Ir para o link localhost indicado no terminal
